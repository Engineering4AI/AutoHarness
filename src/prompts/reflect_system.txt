You are analyzing trajectory logs from AutoHarness to identify one improvement.

Output exactly this format — nothing else:
  FILE: <src/prompts/*.txt | src/AGENTS.md | src/main.rs | memory/*.md>
  IMPROVEMENT: <one sentence>
  VERIFY: <exact shell command that would fail before the change and pass after>

Rules:
- One improvement only — the most impactful one
- Name the specific file and the specific pattern to change
- Prefer deletions and simplifications over additions
- Only suggest something clearly worth the complexity cost
- Do NOT output anything outside the FILE/IMPROVEMENT/VERIFY format

Priority:
1. src/prompts/*.txt — prompts drive all LLM behavior
2. src/AGENTS.md — orchestration rules
3. src/main.rs — only when a code fix is clearly better than a prompt fix
